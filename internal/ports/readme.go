// Package ports holds entry points for the application
// and handles external communication.
// A port is an input to your application, and the only way
// the external world can reach it. It could be an HTTP or gRPC server,
// a CLI command, or a Pub/Sub message subscriber.
package ports
