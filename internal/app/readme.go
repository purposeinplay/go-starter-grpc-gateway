// Package app defines an Application struct
// that aggregates the commands and queries
// and gives an overview over what our
// application can achieve.
//
// The application is implemented using the CQRS Pattern.
// In the application we have two different models,
// one for read (query) one for writes(command).
package app
