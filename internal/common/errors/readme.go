// Package errors is a wrapper over the
// "errors" stdlib, the purpose of wrapping
// is to not have import aliases for
// the "errors" packages in the application
// codebase. It also defines
// the application error codes and how we can
// create them if that is the case.
package errors
